export const DEFAULT_QUERY =
  "How do I configure connection pooling for a high-traffic PostgreSQL database?";

export const DEFAULT_CHUNKS: string[] = [
  `PgBouncer is the most widely used connection pooler for PostgreSQL and sits between your application and the database to reuse a small set of physical connections across many client requests. For high-traffic applications, transaction pooling mode gives the best throughput because it returns a connection to the pool as soon as a transaction commits, rather than holding it for the lifetime of a client session. A typical starting configuration sets default_pool_size to roughly 20 connections per database and max_client_conn to a few thousand, since PgBouncer itself is lightweight and can accept far more client sockets than PostgreSQL can accept backend processes. Because PostgreSQL forks a new OS process per connection, each idle connection costs real memory, so keeping the backend pool small and letting PgBouncer absorb connection churn prevents the database from running out of memory under load. Query pipelining, session-level features like prepared statements, and advisory locks behave differently under transaction pooling, so teams should audit their ORM and driver settings before switching modes in production.`,
  `Getting good performance out of a production PostgreSQL database usually involves looking at several different areas at once, and it can be a little overwhelming to know where to start, especially for teams that are just getting used to running databases at scale. Indexing strategy matters a great deal, and it is generally a good idea to review slow query logs on a regular basis to catch missing indexes before they become a real problem. Vacuuming and autovacuum tuning are also important, since bloat can quietly degrade performance over time in ways that are not always obvious at first glance. Somewhere in the middle of all this, connection handling deserves attention too: opening a brand new connection for every request is expensive, and many teams eventually introduce a pooler in front of the database once traffic grows, though exactly when to do that varies quite a bit from one application to another. Memory settings like shared_buffers and work_mem round out the usual list of knobs worth revisiting periodically.`,
  `A solid backup strategy is one of the most overlooked parts of running any production database, and PostgreSQL is no exception to that rule. Most teams rely on a combination of continuous WAL archiving and periodic base backups, often using tools like pgBackRest or the built-in pg_basebackup utility to capture a consistent snapshot of the cluster. It is worth testing restores regularly, not just taking backups, because a backup that has never been restored successfully is not something you can actually depend on when an incident happens. Retention policies should balance storage cost against how far back you might realistically need to recover, and many teams keep a rolling thirty-day window alongside a handful of longer-term monthly snapshots. Backups also interact with connection limits in a minor way, since a base backup process does briefly occupy a connection slot during the initial copy phase. Encrypting backups at rest and storing them in a separate region from the primary cluster are both standard practice for disaster recovery.`,
];
